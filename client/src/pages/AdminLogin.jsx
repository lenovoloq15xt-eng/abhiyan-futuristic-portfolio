import { useState } from "react";
import { useNavigate, Link } from "react-router-dom";
import toast from "react-hot-toast";
import api from "../utils/api.js";
const AdminLogin=()=>{const nav=useNavigate();const[form,setForm]=useState({email:'',password:''});const submit=async e=>{e.preventDefault();try{const{data}=await api.post('/auth/login',form);localStorage.setItem('abhiyan_token',data.token);toast.success('Welcome back, Abhiyan.');nav('/admin/dashboard')}catch(err){toast.error(err.response?.data?.message||'Login failed')}};return <main className="grid min-h-screen place-items-center bg-void px-5"><form onSubmit={submit} className="glass w-full max-w-md rounded-[1.75rem] p-8"><h1 className="text-4xl font-black text-white">Admin Login</h1><p className="mt-3 text-white/60">Manage Abhiyan's portfolio.</p><input className="mt-8 w-full rounded-2xl bg-black/25 px-4 py-3 text-white" placeholder="admin@abhiyan.dev" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/><input className="mt-4 w-full rounded-2xl bg-black/25 px-4 py-3 text-white" type="password" placeholder="********" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/><button className="mt-6 w-full rounded-full bg-neon px-6 py-3 font-black text-slate-950">Enter Dashboard</button><Link to="/" className="mt-5 block text-center text-neon">Back to site</Link></form></main>};
export default AdminLogin;
